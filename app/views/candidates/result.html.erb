<h1>投票結果</h1>
<h2>候選人列表</h2>

<table>
  <thead>
    <tr>
      <td>候選人姓名</td>
      <td>政黨</td>
      <td>政見</td>
      <td>得票數</td>
    </tr>
  </thead>
  <tbody>
    <% @candidate.each(@candidate) do |candidate| %>
    <tr>
      <td><%= candidate.name %>(年齡：<%= candidate.age %> 歲)</td>
      <td><%= candidate.party %></td>
      <td><%= candidate.politics %></td>
      <td><%= candidate.vote_logs.count %></td>
    </tr>
    <% end %>
  </tbody>
</table>
<%= link_to '回候選人列表', candidates_path %>